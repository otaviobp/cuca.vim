#!/usr/bin/env python

import sys
import os

if __name__ == '__main__':
    # TODO: Fix this workaround to make cuca an installable project
    sys.path.append(os.path.dirname(os.path.dirname(os.path.realpath(__file__))))
    from cuca_cli import CucaCLI

    sys.exit(CucaCLI().main(sys.argv))
